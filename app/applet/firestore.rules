rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true; // Just for initial dev testing so the user app works seamlessly over Firebase
    }
  }
}
